package 数据结构;
/*
    链表：
    查询慢：链表中地址不是连续的，每次查询元素，都必须从头开始查询
    增删快：链表结构，增加/删除一个一个元素，对链表的整体结构没有影响，所以增删快
    链表可分为：单向链表和双向链表
    单向链表：链表中只有一条链子，不能保证元素的顺序（存储元素和取出元素的顺序可能不一样）
    双向链表：链表中有两条链子，其中有一条链子是用来专门记录元素的顺序，是一个有序的集合

    链表中的每一个元素也被称之为一个节点
    一个节点包含了一个数据源(存储数组)，两个指针域（存储地址）
 */

public class LinkedList {
}
